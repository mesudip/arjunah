# v1 validation record

The audited defects have regression coverage in the revised implementation. This record describes automated evidence, not an independent certification or full provider/MCP interoperability assessment. The supported schema subset and limits are part of SPEC sections 5 and 7.1.

Browser validation uses Chrome for Testing 152.0.7977.75 and Firefox 155.0.1 on arm64 macOS. Other operating systems, declared minimum browser versions, Android, and real MCP services have not been validated here. A real GPT-5.6 Sol tool round trip and the live Brave sample site were verified on 2026-09-12; see [PROGRESS.md](PROGRESS.md) for the scope and compatibility limitation.

| Requirement                                          | Automated evidence                                                                                                                                                                                                                                                                                                                                                                        |
| ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Immutable discovery and version consistency          | Both browser E2E descriptor assertions; static package/manifest/API/type checks                                                                                                                                                                                                                                                                                                           |
| Exact-origin grants and top-level scope              | Both browser suites; background rejection of subframes                                                                                                                                                                                                                                                                                                                                    |
| Document and registration isolation                  | Chrome security E2E for delayed navigation/replacement; Firefox delayed cross-origin navigation; background cancellation tests                                                                                                                                                                                                                                                            |
| Credential non-disclosure and origin-bound retention | Provider tests; background Save/Test/remove-key tests; Chrome security settings test                                                                                                                                                                                                                                                                                                      |
| Serialized grants and effective revocation           | Background concurrent approvals, stale-write/revocation, clear-all, and revocation-during-tool tests; both browser denial/revocation paths                                                                                                                                                                                                                                                |
| Direct model requests and permitted model set        | Both browser suites; actual background/provider wire-format regression and unlisted-model rejection                                                                                                                                                                                                                                                                                       |
| Bounded page context and tool output                 | Browser context field assertions; escaped 20,000-character context and 65,536-byte tool result round-trip; UTF-8 boundary tests                                                                                                                                                                                                                                                           |
| Full prompt and tool disclosure                      | Chrome security DOM assertions; both browsers' changed-contract denial and two-stage MCP approval flows                                                                                                                                                                                                                                                                                   |
| Independent contract/resource enforcement            | Background refusal of unapproved contract/MCP origin/tool set, changed remote metadata, and foreign preparation token                                                                                                                                                                                                                                                                     |
| Popup launcher (no popup-owned chat)                 | Both browser suites assert the popup has no prompt and reports protocol support; background rejects the removed popup chat method                                                                                                                                                                                                                                                         |
| Hosted tools and alias routing                       | Both browser suites; long site names, colliding/long MCP names, and aggregate-limit rejection in background tests                                                                                                                                                                                                                                                                         |
| Schema and argument validation                       | Validator tests for malformed/unsupported schemas and nested argument constraints; no-invocation tests for malformed JSON/missing/wrong argument types                                                                                                                                                                                                                                    |
| Safe provider/tool failures                          | Provider HTTP/JSON/shape/tool-call/timeout/oversize tests; throwing/oversized tool result tests                                                                                                                                                                                                                                                                                           |
| MCP transport                                        | Session lifecycle, multiline/chunked/open SSE, notification skipping, ID mismatch, session expiry/reinitialization, and pagination tests; both browser mock MCP rounds                                                                                                                                                                                                                    |
| Automatic/resizable widget                           | Both browser focus assertions plus static resize check; resizing is not exhaustively tested across viewport sizes                                                                                                                                                                                                                                                                         |
| Chrome/Firefox packaging                             | Chrome worker discovery; Firefox temporary installation; zero-warning Mozilla lint; portable JavaScript archive generation                                                                                                                                                                                                                                                                |
| Desktop companion pairing and guards                 | Desktop tests: loopback host/origin rejection, extension CORS, wrong-code rejection, hashed token storage with mode 0600, revocation; background tests for pairing without token exposure                                                                                                                                                                                                 |
| Desktop provider selection and routing               | Background tests: unavailable providers rejected, desktop model exposed by `models.list`, generation routed with bearer token, unlisted model rejected, consent label; provider dispatch unit test                                                                                                                                                                                        |
| Bridged tool rounds                                  | Desktop tests for MCP initialize/list/call with session token and paused-then-resumed calls; background hosted-chat test through a desktop provider; Desktop E2E fake agent tool round; live OpenCode and Codex round trips recorded in PROGRESS.md                                                                                                                                       |
| Desktop image input                                  | Desktop tests: attachments reach the adapter for a vision provider and not without one, bounds and non-user rejection, the newest-first collector, Claude Code's stream-json message and Codex's `-i` files, OpenCode pinned off; provider test for images beside the flattened text; live fresh and resumed round trips in PROGRESS.md                                                   |
| Configuration sync                                   | Desktop revision/masking tests; background push on save/select and pull of newer revisions, including safe rejection of malformed desktop payloads                                                                                                                                                                                                                                        |
| Access levels and exposed catalog                    | Validation tests for `level` expansion; background tests: level 1 sees one model and rejects others, level 2 lists exposed providers without account details, `site.update` narrows providers, unpinned sites follow the global default; visual run: consent dialogs with model picker and provider checkboxes                                                                            |
| Widget options                                       | Validation tests for controls/suggestions/theme/tool-call-view values and control values; background test that disclosed controls reach the model and `model:false` controls do not; Chrome E2E checks the detailed tool presentation; visual run: drawer toggle → `onControlChange`, `chat.setControls`, `invocation.controls`                                                           |
| Image parts, reasoning, attachments                  | Validation tests for part shapes and bounds; background test converting parts to `image_url` and refusing images for non-vision models before any request; visual run: attachment strip, reasoning block, generated image rendering                                                                                                                                                       |
| Hosted activity and usage                            | Background test for `arjunah-progress` event sequence bound to the initiating tab; visual run: thinking indicator, tool cards, token/history/quota footer; popup wallet assertions in Chrome and desktop suites                                                                                                                                                                           |
| Extension-collected tool inputs                      | Validation tests for declaration bounds, model/property separation, scalar schemas, and direct-generation rejection; Chrome E2E enters a masked value through extension UI, completes the site tool round, and asserts the value never appears in captured provider requests                                                                                                              |
| Transcript cards                                     | Card validator tests for the node set, bounds, actions, and form values; broker tests that a card reaches the widget event but never a provider message; hosted E2E clicks a local action inside the closed shadow root and asserts the replacement card and that no model request followed; widget E2E covers both action kinds against a mock backend                                   |
| Tool progress                                        | Page API and content-script caps (200 characters, 50 reports); hosted E2E asserts the progress text never appears in captured provider requests; widget E2E renders a backend `progress` event and a client tool's own report                                                                                                                                                             |
| Site-owned threads                                   | Validation test that declaring a store changes the contract; broker test that supplied history reaches the model between untrusted markers and plain history does not; hosted E2E drives list/create/append through the page bridge and asserts the whole turn was stored; widget E2E switches, creates, and replays a backend thread                                                     |
| Declared remote tools                                | Validation tests for declaration bounds and the schema subset; broker tests that no `tools/list` is issued, that `params._meta` carries the conversation id, that one approval suffices, and that a changed declaration forces fresh consent                                                                                                                                              |
| Standalone renderer                                  | Static check that the published renderer is byte-identical to the extension's and references no extension API, network, or import; widget E2E runs the section 14 event stream, a client tool round, cards, progress, and thread switching in a real browser with no extension installed                                                                                                  |
| Structured site-tool images                          | Independent result validation and declaration tests; background tests for non-vision fallback and vision-message ordering; Chrome fixture returns a known WebP and asserts the textual tool result is followed by the broker-generated user image without base64 in activity DOM                                                                                                          |
| Model and effort picker                              | Renderer unit test for the compact context-window label; widget E2E asserts the picker is drawn from the site catalog, that the effort control follows the selected model's `reasoningLevels`, that `onModelChange` reports both, and that the choice reaches the turn body; Chrome E2E still finds the menu in the wallet's closed shadow root after the control moved into the renderer |
| Entity mentions                                      | Renderer unit tests that a mention flattens to its label and never carries its id to a provider; widget E2E types `@`, asserts the bounded query reached the backend route, inserts a chip, and checks the submitted content carries the `mention` part in position and that a transcript chip reaches the host                                                                           |
| Run phases and diagnostic logs                       | Desktop tests: the companion emits a phase before detection and before launch, text already collected is never merged into, `/api/logs` needs a pairing and clears on demand, the buffer is bounded and sequenced, and a logged command line drops long values; adapter tests for the Codex and Claude Code lifecycle phases; background test that a phase reaches the widget as `agent.phase`, that both logs carry no model output, and that only extension pages may read them; widget E2E asserts the phase becomes the live turn label; desktop E2E reads both logs in the settings page                                                    |
| Standalone collected inputs and host callbacks       | Renderer unit tests for the scalar validator; widget E2E opens the renderer-owned prompt from a client tool, rejects a value that fails the declared schema, asserts the accepted value never appears in the transcript or the tool result, and checks the turn, thread and model callbacks plus the mounted controller's methods                                                         |

Run `npm ci`, install test browsers if needed, then `npm run validate`. `npm run test:e2e:hosted` covers the hosted wallet surfaces and `npm run test:e2e:widget` the standalone renderer; `npm run test:e2e:desktop` runs the desktop companion suite with a fake agent; `ARJUNAH_E2E_LIVE=<provider>` uses a real signed-in CLI. `npm run pack` produces unsigned development archives. Browser install commands select available versions; use `CHROME_PATH` and `FIREFOX_PATH` to reproduce particular binaries.

The browser fixtures and mock endpoints use no real account or credential. A passing test for visible context does not prove exhaustive extraction behavior across every page/layout. MCP scope excludes server-request handling, OAuth, resources, prompts, sampling, and transport resumability. Schema validation intentionally rejects unsupported assertions rather than claiming all JSON Schema dialects.

See [REMEDIATION.md](REMEDIATION.md) for the audit-to-fix mapping and [AUDIT.md](AUDIT.md) for historical findings.

The browser suites stage disposable extension copies with a test-only provider transport redirect. Production endpoint validation still runs against the canonical OpenAI URL, and no live credentials or real provider traffic are used in those suites.
